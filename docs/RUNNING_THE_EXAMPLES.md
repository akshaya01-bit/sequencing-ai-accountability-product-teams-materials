# Running the synthetic examples

This repository provides a synthetic, fully runnable artifact for the study:

> **Sequencing AI Recommendations and Accountability Practices in Product Teams**

The goal is to make the empirical logic of the study inspectable and executable
without exposing any real partner data.

---

## 1. Dependencies

You will need:

- Python 3.8+
- The following Python packages:

```bash
python3 -m pip install --upgrade pip
python3 -m pip install pandas statsmodels scikit-learn joblib
